#lang racket
(define lista(list 0 1 2 3 4 5 6 7))
(define (Cabezera lista)
  (car lista))
(define (Cuerpo lista)
  (cdr lista))
(define (agregar elemento lista)
  (cons elemento lista))

(Cabezera lista)
(Cuerpo lista)
(agregar 10 lista)